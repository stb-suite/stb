# 4.99 — Workflow: Demo Extension (`stb-demoext`)

A guide that comes from an extension repository, not from this one. It exists only to
test that the documentation build picks up extra sources.

## What it shows

- the page appears in the sidebar under Workflows, in numeric order;
- its footer links to the extension's own repository;
- it has no "edit this page" link, because its source is not in this repository.
